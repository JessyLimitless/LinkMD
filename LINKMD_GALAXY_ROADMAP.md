# LinkMD: Galaxy Edition — Claude Code 실행 로드맵

> "파일 저장소에서 살아있는 지식 네트워크로"
> AI와 나눈 대화를 비즈니스 자산으로, 그리고 탐험 가능한 우주로.

---

## 🎯 Overview

LinkMD 기존 메뉴(문서 관리/변환/아카이빙)는 그대로 유지하면서,
**"Universe"** 라는 새로운 메뉴를 추가하여 3D 지식 우주 시각화를 제공한다.

### 메뉴 구조
```
LinkMD
├── 📁 Documents    ← 기존 (MD 관리, 변환, 아카이빙)
├── 🌌 Universe     ← NEW (3D 지식 네트워크 시각화)
├── 🔄 Convert      ← 기존 (docx, hwp 변환)
└── ⚙️ Settings     ← 기존
```

---

## 📋 CLAUDE.md (프로젝트 루트에 추가/수정)

아래 내용을 `CLAUDE.md` 최상단에 추가하세요. 이것이 Claude Code의 헌법입니다.

```markdown
# Project: LinkMD Universe (Galaxy Edition)

## 1. Project Goal
- LinkMD는 단순한 MD 아카이빙이 아닌, 문서 간의 관계를 시각화하는 '지식 탐험' 서비스다.
- 사용자는 3D 우주 공간(Canvas)을 통해 자신의 지식이 어떻게 연결되어 있는지 직관적으로 파악한다.
- 기존 Documents/Convert 메뉴는 그대로 유지하고, Universe 메뉴를 새로 추가한다.

## 2. Tech Stack & Architecture
- Frontend: React, Tailwind CSS
- Visualization: `react-force-graph-3d` (필수: Three.js 기반 네트워크 그래프)
- Data Structure:
  - Nodes: 개별 MD 문서 (Title, Tag, Project, CreatedAt)
  - Links: 같은 Project 또는 같은 Tag를 공유하는 문서끼리 연결
  - Weight: 공유하는 태그/프로젝트가 많을수록 연결 강도 증가

## 3. Design System (Dark Universe)
- Base Theme: Deep Space Black (#050505) — Universe 메뉴 전용
- Sidebar: Glassmorphism (backdrop-filter: blur(20px), bg: rgba(10,10,30,0.7))
- Node Colors:
  - Project 기준 노드: Blue Neon (#00f2ff) + glow effect
  - Tag 기준 노드: Purple Neon (#bd00ff) + glow effect
  - 선택된 노드: White Glow (#ffffff) + pulse animation
  - 비활성 노드: Dimmed (opacity: 0.15)
- Link Colors: rgba(100, 200, 255, 0.15) — 은은한 성간 연결선
- Typography: 'Orbitron' (제목), 'IBM Plex Sans' (본문) — 우주+기술 느낌
- Particle Effect: 링크 위를 흐르는 파티클로 데이터 흐름 표현

## 4. Key Interactions
- 사이드바 Project/Tag 클릭 → 해당 노드 하이라이트 + 카메라 줌인
- 노드 클릭 → 문서 상세 패널 슬라이드인 (Glassmorphism)
- 노드 호버 → 연결된 노드들만 밝아지고 나머지 dimmed
- 마우스 드래그 → 우주 공간 회전
- 스크롤 → 줌인/줌아웃
- 더블클릭 → 해당 문서 편집 모드로 이동 (Documents 메뉴 연계)
```

---

## 🚀 Phase 1: Universe 파운데이션 (Foundation)

### Step 1-1: 라이브러리 설치 및 라우팅

```bash
claude "LinkMD 프로젝트에 react-force-graph-3d 라이브러리를 설치해줘. 
그리고 사이드바 네비게이션에 'Universe' 메뉴를 추가하고, 
/universe 라우트를 만들어줘. 
Universe 페이지는 전체 화면을 차지하는 3D 캔버스 컨테이너로 구성해줘.
배경은 #050505 딥 스페이스 블랙으로."
```

**체크리스트:**
- [ ] `npm install react-force-graph-3d` 완료
- [ ] 사이드바에 🌌 Universe 메뉴 항목 추가
- [ ] `/universe` 라우트 생성
- [ ] UniversePage 컴포넌트 생성 (풀스크린 캔버스)

### Step 1-2: 데이터 모델링 (Graph JSON 생성)

```bash
claude "현재 LinkMD에 저장된 문서 데이터를 분석해서 
react-force-graph-3d가 소비할 수 있는 그래프 JSON을 생성하는 유틸리티를 만들어줘.

규칙:
1. 각 MD 문서는 하나의 Node가 된다 (id, title, tags[], project, createdAt)
2. 같은 Tag를 공유하는 문서끼리 Link를 생성한다
3. 같은 Project에 속한 문서끼리도 Link를 생성한다
4. 공유하는 태그가 많을수록 link의 weight(strength) 값을 높인다

출력 형태:
{
  nodes: [{ id, title, tags, project, group, size }],
  links: [{ source, target, weight, sharedTags }]
}

이 유틸리티를 /utils/graphBuilder.js로 만들어줘."
```

**체크리스트:**
- [ ] `graphBuilder.js` 유틸리티 생성
- [ ] 문서 → 노드 변환 로직
- [ ] 태그 기반 링크 생성 로직
- [ ] 프로젝트 기반 링크 생성 로직
- [ ] weight 계산 로직

### Step 1-3: 3D 그래프 렌더링

```bash
claude "Universe 페이지에 react-force-graph-3d를 연동해서 
graphBuilder에서 생성한 데이터를 3D 그래프로 렌더링해줘.

노드 스타일:
- 기본 색상: #00f2ff (시안 네온)
- 크기: 연결된 링크 수에 비례 (많이 연결된 문서일수록 큰 별)
- 네온 글로우 이펙트 적용 (Three.js PointsMaterial + additive blending)

링크 스타일:
- 색상: rgba(100, 200, 255, 0.15)
- 링크 위를 흐르는 파티클 이펙트 추가 (linkDirectionalParticles)

배경:
- 딥 스페이스 블랙 + 미세한 별 파티클 (starfield) 배경 추가

카메라:
- 초기 위치는 전체 그래프가 보이는 거리
- 마우스 드래그로 회전, 스크롤로 줌"
```

**체크리스트:**
- [ ] ForceGraph3D 컴포넌트 렌더링
- [ ] 노드 커스텀 렌더링 (글로우 이펙트)
- [ ] 링크 파티클 이펙트
- [ ] 별밤 배경 (starfield)
- [ ] 카메라 컨트롤

---

## 🧠 Phase 2: 연결 엔진 & 인터랙션 (Connection Engine)

### Step 2-1: 사이드바 조종석 (Cockpit)

```bash
claude "Universe 페이지 좌측에 Glassmorphism 스타일의 사이드바를 만들어줘.
(backdrop-filter: blur(20px), background: rgba(10,10,30,0.7), border: 1px solid rgba(255,255,255,0.1))

사이드바 구성:
1. 상단: 전체 문서 수, 연결 수 통계
2. Projects 섹션: 프로젝트 목록 (각 프로젝트 옆에 문서 수 배지)
3. Tags 섹션: 태그 목록 (각 태그 옆에 문서 수 배지)  
4. 최근 문서 섹션: 최근 추가된 문서 5개

폰트: 제목은 Orbitron, 본문은 IBM Plex Sans
색상: 텍스트 #e0e0e0, 활성 항목 #00f2ff"
```

**체크리스트:**
- [ ] Glassmorphism 사이드바 컴포넌트
- [ ] 통계 표시 (문서 수, 연결 수)
- [ ] Projects 필터 리스트
- [ ] Tags 필터 리스트
- [ ] 최근 문서 리스트

### Step 2-2: 하이라이트 & 줌인 인터랙션

```bash
claude "사이드바에서 Project나 Tag를 클릭했을 때:

1. 해당 Project/Tag에 속한 노드들만 하이라이트 (원래 색상 유지)
2. 나머지 노드는 dimmed 처리 (opacity: 0.15)
3. 카메라가 하이라이트된 노드 그룹의 중심점으로 부드럽게 줌인 
   (GSAP 또는 requestAnimationFrame으로 smooth transition)
4. 하이라이트된 노드 간의 연결선만 밝게 표시
5. 사이드바에서 '전체보기' 클릭 시 원래 뷰로 복귀

노드 호버 시:
- 해당 노드와 직접 연결된 노드들만 밝아짐
- 툴팁으로 문서 제목, 태그, 프로젝트 표시"
```

**체크리스트:**
- [ ] Project 클릭 → 필터링 + 카메라 이동
- [ ] Tag 클릭 → 필터링 + 카메라 이동
- [ ] Dimmed 처리 (비관련 노드)
- [ ] Smooth camera transition
- [ ] 노드 호버 → 연결 노드 하이라이트
- [ ] 툴팁 표시
- [ ] '전체보기' 리셋 기능

### Step 2-3: 노드 클릭 → 문서 상세 패널

```bash
claude "3D 그래프에서 노드를 클릭하면 우측에서 슬라이드인되는 
문서 상세 패널을 만들어줘.

패널 스타일: Glassmorphism (사이드바와 동일 스타일)
패널 내용:
1. 문서 제목
2. 프로젝트명
3. 태그 목록 (클릭 시 해당 태그 필터링)
4. 문서 내용 미리보기 (MD 렌더링, 최대 500자)
5. 액션 버튼:
   - '📄 문서 열기' → Documents 메뉴의 해당 문서로 이동
   - '🔄 변환하기' → Convert 메뉴로 이동 (해당 문서 선택 상태)
   - '🔗 연결 문서 보기' → 연결된 노드들만 필터링

패널 바깥 클릭 시 닫힘"
```

**체크리스트:**
- [ ] 슬라이드인 패널 컴포넌트
- [ ] MD 미리보기 렌더링
- [ ] 태그 클릭 → 필터링 연계
- [ ] Documents/Convert 메뉴 연계 버튼
- [ ] 연결 문서 필터링
- [ ] 패널 닫기 인터랙션

---

## ✨ Phase 3: 프리미엄 UX (The Wow Factor)

### Step 3-1: 노드 분류 시각화

```bash
claude "노드의 시각적 분류를 강화해줘.

1. 프로젝트별 색상 구분:
   - 각 프로젝트에 고유한 네온 색상 자동 할당
   - 색상 팔레트: ['#00f2ff', '#bd00ff', '#ff006e', '#00ff87', '#ffbe0b']
   - 사이드바에도 프로젝트 옆에 해당 색상 점 표시

2. 노드 크기 = 중요도:
   - 연결(link) 수가 많은 문서 = 큰 노드
   - 최근 수정된 문서 = 약간 더 밝은 글로우

3. 클러스터링:
   - 같은 프로젝트 문서들이 자연스럽게 뭉치도록 forceLink strength 조정
   - 프로젝트 그룹의 중심에 프로젝트 이름 라벨 표시 (CSS2DRenderer)"
```

**체크리스트:**
- [ ] 프로젝트별 색상 자동 할당
- [ ] 노드 크기 = 연결 수 비례
- [ ] 최근 문서 글로우 강화
- [ ] 프로젝트 클러스터링
- [ ] 클러스터 라벨 표시

### Step 3-2: 검색 & 탐색 강화

```bash
claude "Universe 페이지 상단에 검색 바를 추가해줘.

1. 검색 입력 시 실시간으로 매칭되는 노드 하이라이트
2. Enter 키 → 첫 번째 매칭 노드로 카메라 줌인
3. 검색 결과 드롭다운에 매칭된 문서 목록 표시

추가 기능:
- 키보드 단축키: 
  - '/' → 검색 포커스
  - 'Esc' → 필터 초기화
  - 'F' → 전체보기(Fit to screen)
- 미니맵: 우측 하단에 작은 2D 오버뷰 맵 (현재 카메라 위치 표시)"
```

**체크리스트:**
- [ ] 검색 바 (실시간 필터링)
- [ ] 검색 결과 드롭다운
- [ ] 카메라 줌인 to 검색 결과
- [ ] 키보드 단축키
- [ ] 미니맵 (선택사항)

### Step 3-3: 진입 애니메이션

```bash
claude "Universe 페이지에 처음 진입할 때 임팩트 있는 로딩 애니메이션을 추가해줘.

시퀀스:
1. 완전한 블랙 화면
2. 중앙에 'LinkMD Universe' 텍스트가 Orbitron 폰트로 페이드인 (1초)
3. 텍스트 뒤로 별들이 하나씩 점등 (0.5초간 stagger)
4. 카메라가 뒤로 줌아웃하면서 전체 그래프가 서서히 나타남 (1.5초)
5. 노드 간 연결선이 순차적으로 그려짐 (0.5초)
6. 로딩 완료 → 인터랙션 활성화

전체 시간: 약 3.5초
첫 방문 시에만 풀 애니메이션, 이후 방문은 축약 버전 (1.5초)"
```

**체크리스트:**
- [ ] 진입 애니메이션 시퀀스
- [ ] 텍스트 페이드인
- [ ] 별 점등 stagger
- [ ] 카메라 줌아웃
- [ ] 연결선 순차 드로잉
- [ ] 첫 방문/재방문 구분

---

## 🔗 Phase 4: Documents ↔ Universe 연계

### Step 4-1: 양방향 내비게이션

```bash
claude "Documents 메뉴와 Universe 메뉴 간의 양방향 연계를 구현해줘.

1. Documents → Universe:
   - 각 문서 카드에 '🌌 우주에서 보기' 버튼 추가
   - 클릭 시 Universe 페이지로 이동 + 해당 노드로 카메라 줌인

2. Universe → Documents:
   - 노드 더블클릭 → Documents의 해당 문서 편집 모드로 이동
   - 상세 패널의 '문서 열기' → Documents 해당 문서로 이동

3. 상태 동기화:
   - Documents에서 문서 추가/삭제/태그 변경 시 Universe 그래프 자동 업데이트
   - Universe에서의 필터 상태를 URL 파라미터로 관리 
     (예: /universe?project=Cloud5&highlight=doc-123)"
```

**체크리스트:**
- [ ] 문서 카드에 '우주에서 보기' 버튼
- [ ] 노드 더블클릭 → 문서 편집
- [ ] 문서 CRUD → 그래프 실시간 반영
- [ ] URL 기반 필터 상태 관리

---

## 📊 Phase 5: 고급 분석 (Advanced Analytics) — 선택사항

### Step 5-1: 인사이트 패널

```bash
claude "Universe 사이드바 하단에 '인사이트' 섹션을 추가해줘.

표시 내용:
1. '허브 문서' — 가장 많이 연결된 문서 Top 3
2. '고립된 문서' — 아무 연결도 없는 문서 목록 (태그 추가 권고)
3. '강한 연결' — weight가 가장 높은 문서 쌍 Top 3
4. '최근 활동' — 최근 7일간 추가/수정된 문서 타임라인

각 항목 클릭 시 해당 노드로 카메라 이동"
```

**체크리스트:**
- [ ] 허브 문서 분석
- [ ] 고립 문서 감지
- [ ] 강한 연결 쌍 표시
- [ ] 최근 활동 타임라인

---

## ⚡ Quick Start: 지금 바로 실행

터미널에서 아래 순서로 실행하세요:

```bash
# 0. CLAUDE.md 업데이트 (위의 내용을 프로젝트 루트에 추가)

# 1. 라이브러리 설치 + Universe 라우트 생성
claude "CLAUDE.md를 읽고 Phase 1 Step 1-1을 실행해줘"

# 2. 그래프 데이터 모델링
claude "CLAUDE.md를 읽고 Phase 1 Step 1-2를 실행해줘"

# 3. 3D 렌더링 (여기서 "와!" 나옴)
claude "CLAUDE.md를 읽고 Phase 1 Step 1-3를 실행해줘"

# 4. 인터랙션 추가
claude "CLAUDE.md를 읽고 Phase 2를 순서대로 실행해줘"

# 5. 프리미엄 UX
claude "CLAUDE.md를 읽고 Phase 3를 순서대로 실행해줘"
```

---

## 🎯 예상 결과물

### Phase 1 완료 시
- 23개의 별(문서)이 3D 우주 공간에 떠 있음
- 같은 태그를 공유하는 문서끼리 빛나는 선으로 연결
- 마우스로 우주 회전/줌 가능

### Phase 2 완료 시  
- 좌측 조종석(사이드바)에서 프로젝트/태그 클릭 → 카메라가 해당 클러스터로 이동
- 노드 클릭 → 문서 상세 패널 슬라이드인
- 호버 → 연결 관계 하이라이트

### Phase 3 완료 시
- 프로젝트별 색상 구분된 우주
- 임팩트 있는 진입 애니메이션
- 검색 + 키보드 단축키

### 전체 완료 시
- "파일 저장소"에서 "살아있는 지식 우주"로 진화
- Documents ↔ Universe 양방향 탐색
- 지식의 연결 구조를 한눈에 파악

---

> **"23개의 문서가 숫자가 아니라 별이 되는 순간, 
> 지식 관리의 패러다임이 바뀝니다."**
> 
> — LinkMD: Galaxy Edition
